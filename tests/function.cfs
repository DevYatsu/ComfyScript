fn test() {
    let test = "efef"

    return test
}

anon fn (test) {
    let test = test + /* hey dude how r u ? */ "efef" + // comment
     3

    >> test + "str"
}

fn test (value) >> value

let test = test("str")

test(
1
+
2
)

fn closure () {
    >> anon fn (str) {
        return str
    }
}