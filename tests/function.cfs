fn test() {
    let test = "efef"

    return test
}

fn x (test) {
    let test = test +  "efef" + 
     3
/* hey dude how r u ? */
// comment

    >> test + "str"
}

fn test (value) >> value

let test = test("str")

test(
1
+
2
)

(|| {})()

fn closure () {
    >> || {}
}

closure_value = closure()

closure_value()


fn fallibleExpr() -> String? {
    >> "str"
}