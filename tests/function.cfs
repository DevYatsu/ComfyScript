fn test() {
    let test = "efef"

    return test
}

anon fn test(test) {
    let test = test + /* hey dude how r u ? */ "efef" + // comment
     3

    >> test + "str"
}

fn test(value) >> value


test()