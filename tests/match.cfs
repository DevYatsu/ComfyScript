let x = true

match x {
    true => {
        print("it's true")
    }
    false => {
        print("it's false")
    }
}

let str = "string"

match str {
    "value" => {}
    "string" => {
        // here should work
    }
    _ => {
    }
}

import * as fs from "fs"
let file_content = fs.read_file("doesNotExist.txt")

match file_content {
    Ok(content) => {
        print(#"Here is the file content:\n { content }")
    }
    Err(error) => {
        print(#"Error: { error }")
    }
}

