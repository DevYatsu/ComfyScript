import read_to_string, write from "fs"

export fn copy_file(file_name: String, other_file_name: String) -> nil ? {
    let content = read_to_string(file_name)?

    write(other_file_name, content)?
}
